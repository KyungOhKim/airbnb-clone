{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@airbnb-clone/common,@airbnb-clone/server}",
    "build:web": "lerna run build --scope={@airbnb-clone/common,@airbnb-clone/web}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
